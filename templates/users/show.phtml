<a href="/">hello</a>
<a href="/users">all users</a>
<form action="/users", method="GET">
    <input type="search" name="term" value="<?= htmlspecialchars($term) ?>" />
    <input type="submit" valeu="Search" />
</form>
<?php foreach ($users as $user): ?>
<ul>
    <li>
        <?=htmlspecialchars($user) ?>
    </li>
</ul>
<?php endforeach; ?>
